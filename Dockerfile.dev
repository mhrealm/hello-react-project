# 基于官方 Node 镜像（选择和你本地一致的 Node 版本）
FROM node:22.20

# 设置工作目录（容器内的目录）
WORKDIR /app

# 复制 package.json 和 package-lock.json（先复制依赖文件，利用 Docker 缓存）
COPY package*.json ./

# 安装项目依赖
RUN npm install

# 复制项目所有文件到容器工作目录
COPY . .

# 暴露 React 开发服务器默认端口（3000）
EXPOSE 3000

# 启动开发服务器（npm start）
CMD ["npm", "start"]